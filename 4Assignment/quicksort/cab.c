#include <constraints.h>

int main(int argc, char const *argv[])
{
    ll N ;
    scanf("%lld",&N);
    ll Array[N];
    for (ll n = 0; n < N; n++)
    {
        scanf("%lld",&Array[n]);
    }
    
    return 0;
}

